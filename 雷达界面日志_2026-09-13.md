# 游戏活动雷达界面日志 — 2026-09-13（R56 双时间分区版）

> 本日志记录雷达界面最终达成的效果与设计规则，作为后续迭代的基准口径。

## 一、最终界面效果（三区结构 · 清单式简洁风）

### ① 雷达速报（页面最顶部 · 只认 T0）

- 标题「近 7 天新捕获 · N 条」，**只显示紫色 T0**（`T0 09-10`），不显示 T1
- 收录范围：全部游戏中 T0（建页/创建）距今 ≤ 7 天的活动，**按 T0 最新在前**
- 超过 8 条时折叠为「展开其余 N 条 ▾」，点击全展开；无新捕获时收成一行提示
- 语义：刚捕获到什么，第一眼看到

### ② 吸顶工具栏（滚动时钉在顶部）

- **即时搜索**：搜游戏/活动/奖励关键词，命中词紫色高亮、命中分区自动展开，清空恢复
- **渠道 Tab**：全部 53 / 好游快爆 17 / 4399 16 / OPPO 20，一键过滤
- **只看新捕获** 开关：勾上后只剩近 7 天建页的活动
- 三件工具可叠加（如「渠道=OPPO + 搜索Q币」= 只看 OPPO 的 Q币活动）
- 注：排序切换下拉已移除（R56 起排序规则固定，见下）

### ③ 游戏分区 ×25（档案层 · 只认 T1）

- **只显示白色 T1**（`T1 09-12`），T0 不再出现在分区内
- 分区内按**渠道分组**：发丝线 + 渠道色标（好游快爆=橙、4399=蓝、OPPO=绿）
- 每行 = 渠道色点 + 活动名 + `T1 日期`，无卡片边框、无底色块
- 分区头：游戏名 + 活动数 +「新 T1 09-12」白色徽章（该游戏有近 7 天内开始的活动时）
- 有新 T1 的分区自动展开并置顶；其余默认收起，点击分区头展开/收起
- 行尾悬停：⧉ 复制链接、↗ 电脑打开

## 二、排序铁律（R56 用户口径）

| 区域 | 只显示 | 排序规则 |
|---|---|---|
| 速报区 | 紫色 T0 | T0 最新在前（降序） |
| 游戏分区 | 白色 T1 | 分区/渠道组/活动行三级均按 T1 最新在前（降序） |

- **新活动捕获后自动以最新时间排列**（每次运行生成器时按当时数据重排）
- 两个时间彻底分家：T0 只在速报、T1 只在分区，互不干扰
- T2（结束时间）不上屏，仅保留在数据中；已结束活动（T2 < 当前时间）不显示

## 三、时间数据铁律（贯穿采集→校对→展示）

1. **管线分层**：三渠道各自独立采集（各自含筛选）→ 统一关键词总筛选 → 雷达汇总
2. **T0 永不受校对影响**：T0 只来自各渠道自身源头痕迹
   - 好游快爆：URL 建页期
   - 4399：API `stime`（独立字段落表，校对只另存 `orig_*`）
   - OPPO：schema.js 建页配置（早于页面规则时标记「提前建页」，如三角洲行动 08-28 建页 / 09-04 开跑）
3. **T1 校对只动 T1**：快爆+4399 同游戏同 T1 形成共识，且结束日期 ±1 天匹配（证明同一场活动）时，OPPO 的 T1 按共识填；仅 OPPO「即日起」类模糊 T1 参与跨渠道校验
4. 校验结果只记录在数据字段（`t1_source`），页面不显示

## 四、运行方式

- **一键 RUN**：VS Code 打开项目根目录的 `一键RUN雷达.py` → 点运行
  自动完成：三渠道采集（先快爆+4399 并行、后 OPPO）→ 生成看板 → 浏览器打开
- 零第三方依赖（纯 Python 标准库），所有文件与输出固定在 `assets/` 目录
- 网站为**本地静态 HTML**（`assets/radar_board.html`），非云端公网站点

## 五、验证记录（2026-09-13 15:34）

- 速报区：T0 ×14 条、T1 出现 0 次 ✅
- 游戏分区：T1 ×60 行、T0 出现 0 次 ✅
- 19 个分区按最新 T1 严格降序（前 5：和平精英 → 失控进化 → 元梦之星 → 洛克王国：世界 → 使命召唤手游）✅
- 残留检查：new-t0 徽章 0 处、紫点 0 处、排序下拉 0 处 ✅

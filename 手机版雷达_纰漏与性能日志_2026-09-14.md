# 手机版雷达 · 纰漏与性能整合日志（2026-09-14）

> 覆盖范围：2026-09-13 ~ 09-14 的 R62 ~ R65 四轮修复与性能决策。
> 结论先行：所有已知纰漏已修复并真机验证（Redmi K60 / Android 12 / MIUI 13）。

## 一、纰漏与修复清单

| # | 纰漏 | 根因 | 修复 | 验证 |
|---|------|------|------|------|
| R62-1 | Android Studio 构建报 non-ASCII path | 项目路径含中文 | `gradle.properties` 加 `android.overridePathCheck=true` | 构建通过 |
| R62-2 | 报 Chaquopy requires ndk.abiFilters | Chaquopy 15 强制要求 | `defaultConfig` 加 `ndk { abiFilters "armeabi-v7a","arm64-v8a" }` | 构建通过 |
| R63 | 点「⚡本机采集」失败 | ① `runpy.run_path` 在 APK 内无 `.py` 源文件必崩；② `radar/` 子包未打进 APK；③ 数据落到不可写虚拟路径 | 脚本平铺到 `src/main/python/` 根；`radar_entry.py` 改 `importlib.import_module`；数据落点用 `RADAR_DATA` 环境变量指向 `getFilesDir()` | 真机采集 80s 全通过，看板 45 活动 |
| R64-1 | 看板时间无秒级 | 4399 API `stime` 本是秒级，但爬虫 `strftime("%m-%d %H:%M")` 丢秒 | 格式改 `%m-%d %H:%M:%S`；快爆/OPPO 只有日期级 → 显示「无」（不造假精度） | 真机看板显示 `T1 09-10 10:00:00` / `T0 09-10 无` |
| R64-2 | 进度条肉眼看不见 | ① 高度仅 16px；② 系统 tint 在 MIUI 不生效 | 自绘 `LayerDrawable`（深灰轨道+荧光绿 #00E676），高 40px | 真机 UI dump 确认渲染 |
| R64-3 | APK 重装后闹钟丢失 | Android 更新包时清除 PendingIntent 闹钟 | `BootReceiver` 监听 `MY_PACKAGE_REPLACED` + 打开 App 时自动补排 | 重装后 dumpsys 确认 HOURLY_CRAWL 恢复 |
| R65-1 | **清后台重开 App 显示旧数据** | `onCreate` 无条件 `loadUrl(云端页)`，本机看板被无视 | 启动时若有本机看板 → 恢复并显示采集时间；`onResume` 检测到后台有新采集 → 自动刷新 | force-stop 重开，UI 确认「已恢复上次本机采集结果」 |
| R65-2 | 手动 `am broadcast` 测试报 ForegroundServiceStartNotAllowed | Android 12+ 后台启动前台服务需精确闹钟豁免 | 非生产 bug：真实 AlarmManager 闹钟触发自带豁免 | 真实闹钟链路真机跑通 |

## 二、性能决策（重要）

### 已删除：永久历史库 radar_history.jsonl
- **决策**：按用户要求整体移除（代码、调用、页眉展示、手机端残留文件）。
- **原因**：用户不希望任何"只增不减"的数据结构长期驻留。虽然原实现（按活动 id 去重、每条只写一次、每次采集线性读一遍）在一年量级（约数百 KB、毫秒级读取）不会造成可感知的性能退化，但考虑采集时间窗口敏感、App 长期轻量运行的诉求，删除是更稳妥的选择。

### 保留：7 天时间窗口机制（不受影响）
- `radar_state.json` 的 `first_seen` 记录：**每条记录 7 天自动过期清除**，数据规模有上界（≈一周内出现的活动数，几十条），永不膨胀。
- 看板"新捕获 7 天"口径、T0 速报 ≤7 天口径均基于此，逻辑未动。

### 其他性能保障
- 云端（GitHub Actions）与手机端脚本保持同一份逻辑（assets/ 与 手机端 python/ 同步），避免分叉维护。
- 手机端采集约 80~96 秒完成（快爆→4399→OPPO→看板），前台服务 + 通知进度条，后台整点自动执行不打开 UI。

## 三、当前功能一览

- ⚡本机采集：手机直抓三渠道，~90 秒，绿色进度条 + 阶段数字。
- ☁️云端：GitHub Pages 公网站点（linwenjiezz.github.io/true-reward-crawler）。
- 🌙每日0点 / ⏰整点采集：AlarmManager 精确闹钟（自动跳过 00:00 重复），后台静默执行，通知栏进度条，跑完自动更新 App 内看板。
- 🔄看板留存：重开 App 自动恢复上次本机采集结果，跨重启不丢。
- 🔧权限：MIUI/HyperOS 自启动 + 电池白名单引导，换机自动重新检测。
